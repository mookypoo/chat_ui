import 'package:flutter/material.dart';

class AndroidChatList extends StatelessWidget {
  const AndroidChatList({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
