import 'package:flutter/material.dart';

class AndroidChatDetail extends StatelessWidget {
  const AndroidChatDetail({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
